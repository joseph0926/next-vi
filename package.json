{
  "name": "next-vi",
  "version": "0.1.0",
  "private": true,
  "description": "Next.js Routing/Cache Visual Inspector",
  "license": "MIT",
  "packageManager": "pnpm@10.29.2",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "turbo": "^2.8.9",
    "typescript": "^5.9.3"
  }
}
